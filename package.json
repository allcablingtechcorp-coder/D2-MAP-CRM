{
  "name": "d2-map-crm-workspace",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "test:functions": "npm --prefix functions run test && npm --prefix functions run build",
    "test:rules": "firebase emulators:exec --project demo-d2-map-crm --only firestore \"npm --prefix rules-tests test\"",
    "test:integration": "firebase emulators:exec --project demo-d2-map-crm --only firestore \"npm --prefix functions test -- src/callables.integration.test.ts src/companyWorkspaces.integration.test.ts\""
  },
  "devDependencies": {
    "firebase-tools": "15.10.1"
  }
}
